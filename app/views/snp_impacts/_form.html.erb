<%= form_with(model: snp_impact) do |form| %>
  <% if snp_impact.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(snp_impact.errors.count, "error") %> prohibited this snp_impact from being saved:</h2>

      <ul>
        <% snp_impact.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group row">
    <%= label_tag "Name", nil,  {:class => 'col-sm-2 col-form-label'} %>
    <div class="col-sm-10">
      <%= form.text_field :name, {:class => 'form-control'} %>
    </div>
  </div>
  
  
  <div>
    <%= form.submit %>
  </div>
<% end %>
