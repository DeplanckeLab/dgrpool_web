<%= form_with(model: var_type) do |form| %>
  <% if var_type.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(var_type.errors.count, "error") %> prohibited this var_type from being saved:</h2>

      <ul>
        <% var_type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
