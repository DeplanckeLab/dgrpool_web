<%= form_with(model: snp_type) do |form| %>
  <% if snp_type.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(snp_type.errors.count, "error") %> prohibited this snp_type from being saved:</h2>

      <ul>
        <% snp_type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
