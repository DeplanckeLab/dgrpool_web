<%= form_with(model: human_ortholog) do |form| %>
  <% if human_ortholog.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(human_ortholog.errors.count, "error") %> prohibited this human_ortholog from being saved:</h2>

      <ul>
        <% human_ortholog.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
