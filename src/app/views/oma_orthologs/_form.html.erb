<%= form_with(model: oma_ortholog) do |form| %>
  <% if oma_ortholog.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(oma_ortholog.errors.count, "error") %> prohibited this oma_ortholog from being saved:</h2>

      <ul>
        <% oma_ortholog.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
