<%= form_with(model: figure_type) do |form| %>
  <% if figure_type.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(figure_type.errors.count, "error") %> prohibited this figure_type from being saved:</h2>

      <ul>
        <% figure_type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group row">
    <%= label_tag "Name", nil,  {:class => 'col-sm-2 col-form-label'} %>
    <div class="col-sm-10">
      <%= form.text_field :name, {:class => 'form-control'} %>
    </div>
  </div>

  
  <div>
    <%= form.submit %>
  </div>
<% end %>
