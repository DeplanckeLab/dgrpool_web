<%= form_with(model: gene) do |form| %>
  <% if gene.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(gene.errors.count, "error") %> prohibited this gene from being saved:</h2>

      <ul>
        <% gene.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
