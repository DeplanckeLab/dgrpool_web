<%= form_with(model: flybase_allele) do |form| %>
  <% if flybase_allele.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(flybase_allele.errors.count, "error") %> prohibited this flybase_allele from being saved:</h2>

      <ul>
        <% flybase_allele.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
