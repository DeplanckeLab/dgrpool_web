<%= form_with(model: gwas_result) do |form| %>
  <% if gwas_result.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(gwas_result.errors.count, "error") %> prohibited this gwas_result from being saved:</h2>

      <ul>
        <% gwas_result.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
