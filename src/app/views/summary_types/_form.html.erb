<%= form_with(model: summary_type) do |form| %>
  <% if summary_type.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(summary_type.errors.count, "error") %> prohibited this summary_type from being saved:</h2>

      <ul>
        <% summary_type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

   <div class="form-group row">
    <%= label_tag "Label", nil,  {:class => 'col-sm-2 col-form-label'} %>
    <div class="col-sm-10">
      <%= form.text_field :name, {:class => 'form-control'} %>
    </div>
  </div>
  
  <div>
    <%= form.submit "Submit", {:class => 'btn btn-success'}%>
  </div>
<% end %>
