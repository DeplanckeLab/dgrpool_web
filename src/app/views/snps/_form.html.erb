<%= form_with(model: snp) do |form| %>
  <% if snp.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(snp.errors.count, "error") %> prohibited this snp from being saved:</h2>

      <ul>
        <% snp.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
